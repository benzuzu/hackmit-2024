/** @type {import('next').NextConfig} */
const nextConfig = {};

// next.config.js
export default {
    images: {
      domains: ['via.placeholder.com'],  // Add any other domains here as needed
    },
  };
  
